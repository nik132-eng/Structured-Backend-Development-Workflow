---
description: Branch discipline and safe git operations for feature work
alwaysApply: true
---

# Git Workflow

- Inspect `git status` before starting implementation.
- Find the base branch from the project's convention (its rules/AGENTS.md) or `git remote show origin` (HEAD branch); don't assume `main`.
- Never implement directly on the base branch. Branch first: `cursor/<ticket-or-feature>/<short-slug>`.
- If the working tree is dirty with unrelated changes, ask before any fetch/rebase/reset/checkout that could touch them.
- Commit only when the user asks. Keep the diff coherent — no drive-by edits to unrelated files.
- Compare PRs against the remote base branch (`origin/<base>`).
- Never force-push, hard-reset, or rewrite pushed history without an explicit user request.
